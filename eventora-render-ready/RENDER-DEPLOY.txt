EVENTORA - RENDER READY

IMPORTANT:
Upload the CONTENTS of this folder to the ROOT of your GitHub repository.

Your GitHub repository root should look like:

package.json
server.js
render.yaml
public/
data/
src/
README.md

Do NOT put everything inside another eventora-render-ready folder in GitHub.

RENDER SETTINGS

Root Directory:
Leave BLANK

Build Command:
npm install

Start Command:
npm start

Environment Variables:
JWT_SECRET = any long random secret
ADMIN_EMAIL = admin@eventora.local
ADMIN_PASSWORD = ChangeThis123!

Optional:
RAZORPAY_KEY_ID
RAZORPAY_KEY_SECRET

Then click:
Manual Deploy -> Clear build cache & deploy

Admin:
admin@eventora.local
ChangeThis123!

Change the admin password before a real public launch.
