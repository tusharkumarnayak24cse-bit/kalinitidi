# Push notifications
Push is intentionally not enabled without your Firebase credentials. Create a Firebase Android app for `com.kaalinitidi.game`, download `google-services.json`, then add Capacitor Push Notifications and FCM server credentials. Never commit private server keys. The v16 UI/server remains fully usable without push.
