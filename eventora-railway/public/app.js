const $=id=>document.getElementById(id);let events=[],config={};
async function init(){[events,config]=await Promise.all([fetch("/api/events").then(r=>r.json()),fetch("/api/config").then(r=>r.json())]);$("paymentMode").textContent=config.paymentMode==="razorpay"?"Razorpay enabled":"Demo payment mode";$("eventGrid").innerHTML=events.map(e=>`<article class="event-card"><div class="event-art"></div><div class="event-body"><p class="eyebrow dark">${new Date(e.event_date).toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"})}</p><h3>${e.name}</h3><p class="subtle">${e.venue}</p><p>${e.description||""}</p><div class="availability"><span>${e.sold} sold</span><strong>${e.remaining} remaining</strong></div></div></article>`).join("");$("eventId").innerHTML=events.map(e=>`<option value="${e.id}">${e.name}</option>`).join("");updatePasses();}
function current(){return events.find(e=>e.id===$("eventId").value)}function updatePasses(){const e=current();$("passType").innerHTML=Object.entries(e.passes).map(([n,p])=>`<option value="${n}">${n} — ₹${p.toLocaleString("en-IN")}</option>`).join("");updateTotal()}function updateTotal(){const e=current(),p=$("passType").value,q=Number($("quantity").value||1);$("total").textContent="₹"+((e?.passes?.[p]||0)*q).toLocaleString("en-IN")}
$("eventId").addEventListener("change",updatePasses);$("passType").addEventListener("change",updateTotal);$("quantity").addEventListener("input",updateTotal);
$("bookingForm").addEventListener("submit",async ev=>{ev.preventDefault();$("message").className="message";$("message").textContent="Creating booking...";const payload={eventId:$("eventId").value,name:$("name").value,phone:$("phone").value,email:$("email").value,passType:$("passType").value,quantity:Number($("quantity").value)};const r=await fetch("/api/bookings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await r.json();if(!r.ok){$("message").className="message bad";$("message").textContent=data.error||"Booking failed";return}if(!data.paymentRequired){$("message").className="message ok";$("message").textContent="Demo booking confirmed.";showTicket(data.booking);await init();return}const b=data.booking;new Razorpay({key:data.razorpayKeyId,amount:b.amount*100,currency:"INR",name:"Eventora",description:"Event ticket",order_id:b.razorpay_order_id,prefill:{name:b.guest_name,email:b.email,contact:b.phone},handler:async resp=>{const vr=await fetch("/api/payments/verify",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({bookingId:b.id,...resp})});const vd=await vr.json();if(!vr.ok){$("message").className="message bad";$("message").textContent=vd.error||"Payment verification failed";return}$("message").className="message ok";$("message").textContent="Payment confirmed.";showTicket(vd.booking);await init()}}).open()});
function showTicket(b){$("ticketSection").classList.remove("hidden");$("ticketInfo").textContent=`${b.guest_name} • ${b.pass_type} × ${b.quantity}`;$("ticketId").textContent=b.id;$("qr").src=b.qr_data_url;$("ticketSection").scrollIntoView({behavior:"smooth"})}
init();